spring.application.name=Backend

# 1. Database Connection Settings
spring.datasource.url=jdbc:postgresql://localhost:5432/PASAR
spring.datasource.username=postgres
spring.datasource.password=nickythong

# 2. Hibernate / JPA Settings
# This is the "validate thing" - it checks if your tables match your entities
spring.jpa.hibernate.ddl-auto=validate

# 3. Show SQL in the console (helpful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

